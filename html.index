<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>A Small Thought</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600&family=Handlee&display=swap');

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  overflow: hidden;
  background: linear-gradient(120deg, #fdeff9, #ecf9ff, #fef6e4);
  background-size: 400% 400%;
  animation: bgMove 15s ease infinite;
  font-family: 'Playfair Display', serif;
}

@keyframes bgMove {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* Floating petals */
.petal {
  position: absolute;
  width: 14px;
  height: 14px;
  background: pink;
  opacity: 0.5;
  border-radius: 50%;
  animation: fall linear infinite;
}

@keyframes fall {
  from { transform: translateY(-10vh); }
  to { transform: translateY(110vh); }
}

/* SLIDER */
.slider {
  display: flex;
  width: 1000%;
  height: 100vh;
  transition: transform 0.8s ease;
}

.slide {
  width: 100vw;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* PAPER STYLE */
.paper {
  background: #fffdf7;
  width: 80%;
  max-width: 720px;
  padding: 50px;
  border-radius: 10px;
  box-shadow: 0 20px 40px rgba(0,0,0,0.15);
}

.paper h1 {
  margin-bottom: 20px;
}

.paper p {
  font-family: 'Handlee', cursive;
  font-size: 1.2rem;
  line-height: 1.8;
  white-space: pre-wrap;
}

/* BUTTON */
button {
  margin-top: 30px;
  padding: 12px 30px;
  border-radius: 25px;
  border: none;
  background: #ff8fb1;
  color: white;
  font-size: 1rem;
  cursor: pointer;
  transition: 0.3s;
}

button:hover {
  background: #ff6f9c;
}

/* ENVELOPE */
.envelope {
  width: 260px;
  height: 180px;
  position: relative;
  cursor: pointer;
}

.env-body {
  width: 100%;
  height: 100%;
  background: #ffcad4;
  position: absolute;
  border-radius: 8px;
}

.env-flap {
  position: absolute;
  width: 0;
  height: 0;
  border-left: 130px solid transparent;
  border-right: 130px solid transparent;
  border-bottom: 90px solid #ff9fbf;
  top: -1px;
  transform-origin: top;
  transition: transform 1s ease;
}

.envelope.open .env-flap {
  transform: rotateX(180deg);
}

.env-text {
  position: absolute;
  width: 100%;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 1.2rem;
  color: #6b1d3a;
}

/* POEM DIVIDER */
.hidden {
  display: none;
}
</style>
</head>

<body>

<script>
/* Floating petals */
for (let i = 0; i < 25; i++) {
  let petal = document.createElement("div");
  petal.className = "petal";
  petal.style.left = Math.random() * 100 + "vw";
  petal.style.animationDuration = 6 + Math.random() * 6 + "s";
  document.body.appendChild(petal);
}
</script>

<div class="slider" id="slider">

  <!-- SLIDE 1 -->
  <div class="slide">
    <div class="envelope" onclick="openEnvelope()">
      <div class="env-flap"></div>
      <div class="env-body"></div>
      <div class="env-text">Tap to open 💌</div>
    </div>
  </div>

  <!-- SLIDE 2 -->
  <div class="slide">
    <div class="paper">
      <h1 class="typewriter">Hey Avinya,</h1>
      <p class="typewriter">
This isn’t anything dramatic or important.
Just a quiet pause in a moving world,
where a thought chose to stay a little longer
and turn into words.
      </p>
      <button onclick="next()">Next</button>
    </div>
  </div>

  <!-- SLIDE 3 -->
  <div class="slide">
    <div class="paper">
      <p class="typewriter">
Some connections don’t announce themselves.
They arrive gently,
through simple conversations,
through moments that feel easy
without needing an explanation.
      </p>
      <button onclick="next()">Next</button>
    </div>
  </div>

  <!-- SLIDE 4 -->
  <div class="slide">
    <div class="paper">
      <p class="typewriter">
There’s distance between where we are,
but it hasn’t stopped words from flowing naturally.
Since November 2025,
those simple exchanges have added up
to something quietly meaningful.
      </p>
      <button onclick="next()">Next</button>
    </div>
  </div>

  <!-- SLIDE 5 -->
  <div class="slide">
    <div class="paper">
      <p class="typewriter">
This isn’t a message asking for anything.
Just a gentle reminder that being you, Avinya,
exactly the way you are,
is already more than enough.
      </p>
      <button onclick="showPoem()">Read more</button>
    </div>
  </div>

  <!-- POEM SLIDES -->
  <div class="slide hidden">
    <div class="paper">
      <p class="typewriter">
There is distance — not loud, not heavy.
Words cross it softly,
finding their way into ordinary moments,
making them warmer than before.
      </p>
      <button onclick="next()">Next</button>
    </div>
  </div>

  <div class="slide hidden">
    <div class="paper">
      <p class="typewriter">
Nothing promised, nothing claimed.
Just moments shared as they come,
and thoughts that stay
when the world grows quiet.
      </p>
      <button onclick="next()">Next</button>
    </div>
  </div>

  <div class="slide hidden">
    <div class="paper">
      <p class="typewriter" style="text-align:right;">
— Yatin
      </p>
    </div>
  </div>

</div>

<script>
let index = 0;
const slider = document.getElementById("slider");

function next() {
  index++;
  slider.style.transform = `translateX(-${index * 100}vw)`;
  setTimeout(typeCurrentSlide, 500);
}

function openEnvelope() {
  document.querySelector(".envelope").classList.add("open");
  setTimeout(() => next(), 1200);
}

function showPoem() {
  document.querySelectorAll(".hidden").forEach(el => el.classList.remove("hidden"));
  next();
}

/* TYPEWRITER EFFECT */
function typeCurrentSlide() {
  const currentSlide = document.querySelectorAll(".slide")[index];
  const elements = currentSlide.querySelectorAll(".typewriter");

  elements.forEach(el => {
    const text = el.innerText;
    el.innerText = "";
    let i = 0;
    const speed = 30;

    function typing() {
      if (i < text.length) {
        el.innerText += text.charAt(i);
        i++;
        setTimeout(typing, speed);
      }
    }
    typing();
  });
}
</script>

</body>
</html>
